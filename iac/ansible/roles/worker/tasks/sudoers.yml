---
- name: Install operator sudoers fragment (validated by visudo)
  ansible.builtin.template:
    src: operator-mywebapp.j2
    dest: /etc/sudoers.d/operator-mywebapp
    owner: root
    group: root
    mode: "0440"
    validate: "/usr/sbin/visudo -cf %s"
